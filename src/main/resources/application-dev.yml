server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/projectlamia/config-repo
          username: projectlamia
          password: Vamia123!
          delete-untracked-branches: true
          search-paths: '{application}'
          force-pull: true